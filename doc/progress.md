# 项目进展文档

## 目标
实现程序的独立部署（生成可随时访问的公网网址）。

## 当前阶段任务 (2026-04-25)
- [x] 因考虑到纯前端部署在公网存在 API Key 泄漏风险，将修改代码以支持用户在网页前端输入和保存个人的 Gemini API Key
- [x] 生成静态文件用于构建
- [x] 指导用户进行 Vercel/Netlify 或通过云服务器部署

## 当前阶段任务: 上传 GitHub (2026-04-25)
- [x] 本地初始化 Git 仓库并完成初次提
- [x] 获取用户的远程仓库地址并推送代码

## 性能优化阶段 (2026-04-25)
- [x] 将大模型生成的视觉方案产出量级从 23 个锐减至 12 个（3实景+3模特+4详情+2视频），解决了因为词汇量过多导致分析服装等复杂要素商品时的响应“卡顿”问题。完成提速 50%。

## 平台与时效剥离升级 (2026-04-25)
- [x] 移除了代码、Prompt 及前端 UI 提示中所有关于 Wildberries (WB) 的元素，全面聚焦 Ozon
- [x] 在 AI 生成指令中强制指定当前逻辑年为 2026，避免分析时返回过期的带年份热搜标签（如秋季 2024 等）

## 生效与定位调整 (2026-04-25)
- [x] 根据实际电商卖货需求，已将“生成直观概念图”功能作战略下线处理，重新明确当前产品定位为：最强视觉指令与洞察引擎。不再集成改变商品款式的文生图干扰项。

## 拥堵规避与接口升级 (2026-04-25)
- [x] 由于 Google AI `gemini-3-flash-preview` 免费通道频繁触发 HTTP 503 高并发超时报错，现已在底层架构中将运算挂载点全局切换至 `gemini-1.5-pro`。彻底避开 Flash 请求洪峰，提供稳定高质的生成体验。

## API 安全配置与多环境适配 (2026-05-02)
- [x] 重构前端 API Key 存储逻辑，修正多设备下的 `localStorage` 键值不匹配导致 Key 丢失报错的 Bug。
- [x] 新增用户自定义 AI 模型版本下拉框（支持 `Pro` / `Flash` / `Lite` 等全系内测版本），完美匹配用户特定的 Google AI Studio 内测通道及配额（Quota）权限限制。
- [x] 新增 API 密钥“静默防呆机制”，自动过滤头尾回车与隐藏空格，解决前端用户因复制产生 `400 INVALID_ARGUMENT` 的高频痛点。
